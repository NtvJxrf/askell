// Right-side panel (~60%): order/positions management. Structure-only stub for
// now — the toolbar, summary and positions table will be filled in later.
export function PositionsPanel() {
  return (
    <div className="flex h-full flex-col">
        <p className="text-sm text-zinc-400 dark:text-zinc-500">
          Управление позициями
        </p>
    </div>
  );
}
