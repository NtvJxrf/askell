import { CalculatorWorkspace } from './_components/CalculatorWorkspace';

export default function CalculatorsPage() {
  return (
    <div className="h-full">
      <CalculatorWorkspace />
    </div>
  );
}
